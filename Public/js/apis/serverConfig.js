(function() {
  var serverConfig;

  serverConfig = {
    melot: 'http://localhost:9000/lostisland/index.php/',
    mac: '',
    hp: ''
  };

  LLApi.setServerPre(serverConfig.melot);

}).call(this);
